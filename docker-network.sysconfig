# /etc/sysconfig/docker-network
# It's convenient (but insecure) to have docker listening on a tcp port.
# If you need it, try
#DOCKER_NETWORK_OPTIONS=-H unix:///var/run/docker.sock -H tcp://127.0.0.1:2375
DOCKER_NETWORK_OPTIONS=-H unix:///var/run/docker.sock
